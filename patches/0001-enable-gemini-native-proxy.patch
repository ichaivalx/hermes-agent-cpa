diff --git a/agent/gemini_native_adapter.py b/agent/gemini_native_adapter.py
index 0fb31b7..f938220 100644
--- a/agent/gemini_native_adapter.py
+++ b/agent/gemini_native_adapter.py
@@ -60,13 +60,21 @@ def bare_gemini_model_id(model: str) -> str:
 
 
 def is_native_gemini_base_url(base_url: str) -> bool:
-    """Return True when the endpoint speaks Gemini's native REST API."""
+    """Return True when the endpoint speaks Gemini's native REST API.
+
+    Besides Google AI Studio itself, accept a custom gateway whose base path
+    is the native Gemini API version root.  This keeps OpenAI-compatible
+    ``/openai`` endpoints on the standard client while allowing self-hosted
+    Gemini-native relays such as CLIProxyAPI.
+    """
     normalized = str(base_url or "").strip().rstrip("/").lower()
     if not normalized:
         return False
-    if "generativelanguage.googleapis.com" not in normalized:
+    if normalized.endswith("/openai"):
         return False
-    return not normalized.endswith("/openai")
+    if "generativelanguage.googleapis.com" in normalized:
+        return True
+    return normalized.endswith("/v1beta")
 
 
 def probe_gemini_tier(
diff --git a/hermes_cli/web_server.py b/hermes_cli/web_server.py
index 701c566..bb744f9 100644
--- a/hermes_cli/web_server.py
+++ b/hermes_cli/web_server.py
@@ -6395,13 +6395,27 @@ async def get_model_options(
             # Keep the profile override inside the worker thread so the full
             # sync picker build (config load, pricing, refresh probes) runs
             # off the event loop under the requested profile.
-            with _profile_scope(profile):
-                return build_model_options_payload(
-                    load_picker_context(),
-                    explicit_only=bool(explicit_only),
-                    include_unconfigured=bool(include_unconfigured),
-                    refresh=bool(refresh),
+            with _profile_scope(profile) as scoped_home:
+                from agent.secret_scope import (
+                    build_profile_secret_scope,
+                    reset_secret_scope,
+                    set_secret_scope,
                 )
+                from hermes_constants import get_hermes_home
+
+                profile_home = scoped_home or get_hermes_home()
+                secret_token = set_secret_scope(
+                    build_profile_secret_scope(profile_home)
+                )
+                try:
+                    return build_model_options_payload(
+                        load_picker_context(),
+                        explicit_only=bool(explicit_only),
+                        include_unconfigured=bool(include_unconfigured),
+                        refresh=bool(refresh),
+                    )
+                finally:
+                    reset_secret_scope(secret_token)
 
         return await run_in_threadpool(_build_payload_scoped)
     except HTTPException:
diff --git a/plugins/model-providers/gemini/__init__.py b/plugins/model-providers/gemini/__init__.py
index b136fb2..2d54883 100644
--- a/plugins/model-providers/gemini/__init__.py
+++ b/plugins/model-providers/gemini/__init__.py
@@ -9,15 +9,87 @@ carries the thinking_config translation hook so the transport's profile
 path produces the same extra_body shape the legacy flag path did.
 """
 
+import json
+import logging
+import urllib.request
 from typing import Any
 
 from providers import register_provider
-from providers.base import ProviderProfile
+from providers.base import ProviderProfile, _profile_user_agent
+
+
+logger = logging.getLogger(__name__)
 
 
 class GeminiProfile(ProviderProfile):
     """Gemini — translate reasoning_config to thinking_config in extra_body."""
 
+    def fetch_models(
+        self,
+        *,
+        api_key: str | None = None,
+        base_url: str | None = None,
+        timeout: float = 8.0,
+    ) -> list[str] | None:
+        """Fetch models from a native Gemini endpoint when one is configured.
+
+        Native Gemini returns ``models[].name`` and authenticates with
+        ``x-goog-api-key``.  OpenAI-compatible Gemini endpoints retain the
+        base profile's ``data[].id`` / Bearer behavior.
+        """
+        effective_base = (base_url or self.base_url or "").strip()
+        if not effective_base:
+            return None
+
+        from agent.gemini_native_adapter import is_native_gemini_base_url
+
+        if not is_native_gemini_base_url(effective_base):
+            return super().fetch_models(
+                api_key=api_key,
+                base_url=base_url,
+                timeout=timeout,
+            )
+
+        from hermes_cli.urllib_security import open_credentialed_url
+
+        url = (self.models_url or "").strip()
+        if not url:
+            url = effective_base.rstrip("/") + "/models"
+
+        req = urllib.request.Request(url)
+        if api_key:
+            req.add_header("X-Goog-Api-Key", api_key)
+        req.add_header("Accept", "application/json")
+        req.add_header("User-Agent", _profile_user_agent())
+        for key, value in self.default_headers.items():
+            req.add_header(key, value)
+
+        try:
+            with open_credentialed_url(req, timeout=timeout) as response:
+                payload = json.loads(response.read().decode())
+
+            items = payload.get("models", []) if isinstance(payload, dict) else []
+            models: list[str] = []
+            seen: set[str] = set()
+            for item in items:
+                if not isinstance(item, dict):
+                    continue
+                methods = item.get("supportedGenerationMethods")
+                if isinstance(methods, list) and methods and not any(
+                    str(method).lower() == "generatecontent" for method in methods
+                ):
+                    continue
+                name = str(item.get("name") or "").strip()
+                if name.startswith("models/"):
+                    name = name[len("models/"):]
+                if name and name not in seen:
+                    seen.add(name)
+                    models.append(name)
+            return models
+        except Exception as exc:
+            logger.debug("fetch_models(%s): %s", self.name, exc)
+            return None
+
     def build_extra_body(
         self, *, session_id: str | None = None, **context: Any
     ) -> dict[str, Any]:
diff --git a/tests/agent/test_gemini_native_adapter.py b/tests/agent/test_gemini_native_adapter.py
index ab98fed..01fd0f4 100644
--- a/tests/agent/test_gemini_native_adapter.py
+++ b/tests/agent/test_gemini_native_adapter.py
@@ -19,6 +19,15 @@ class DummyResponse:
         return self._payload
 
 
+def test_native_base_url_accepts_v1beta_gateway_without_matching_google_host():
+    from agent.gemini_native_adapter import is_native_gemini_base_url
+
+    assert is_native_gemini_base_url("https://cpa.example.test/v1beta") is True
+    assert is_native_gemini_base_url("https://cpa.example.test/v1beta/") is True
+    assert is_native_gemini_base_url("https://cpa.example.test/v1") is False
+    assert is_native_gemini_base_url("https://cpa.example.test/v1beta/openai") is False
+
+
 
 
 
diff --git a/tests/hermes_cli/test_web_server_profile_unification.py b/tests/hermes_cli/test_web_server_profile_unification.py
index b1d60df..f12b735 100644
--- a/tests/hermes_cli/test_web_server_profile_unification.py
+++ b/tests/hermes_cli/test_web_server_profile_unification.py
@@ -168,6 +168,33 @@ class TestProfileScopedMcp:
 
 
 class TestProfileScopedModel:
+    def test_model_options_reads_target_profile_key(
+        self, client, isolated_profiles, monkeypatch
+    ):
+        """Catalog refresh must authenticate as the selected profile."""
+        import hermes_cli.inventory as inventory
+        from agent.secret_scope import get_secret
+
+        worker_home = isolated_profiles["worker_beta"]
+        (worker_home / ".env").write_text(
+            "CPA_API_KEY=worker-profile-key\n", encoding="utf-8"
+        )
+        monkeypatch.setenv("CPA_API_KEY", "dashboard-process-key")
+        monkeypatch.setattr(inventory, "load_picker_context", lambda: object())
+        monkeypatch.setattr(
+            inventory,
+            "build_model_options_payload",
+            lambda *_args, **_kwargs: {"key": get_secret("CPA_API_KEY", "")},
+        )
+
+        resp = client.get(
+            "/api/model/options",
+            params={"profile": "worker_beta", "refresh": "true"},
+        )
+
+        assert resp.status_code == 200
+        assert resp.json() == {"key": "worker-profile-key"}
+
     def test_model_set_main_scoped(self, client, isolated_profiles):
         resp = client.post(
             "/api/model/set",
diff --git a/tests/plugins/model_providers/test_gemini_profile.py b/tests/plugins/model_providers/test_gemini_profile.py
index cbb4e25..ce989b5 100644
--- a/tests/plugins/model_providers/test_gemini_profile.py
+++ b/tests/plugins/model_providers/test_gemini_profile.py
@@ -2,6 +2,9 @@
 
 from __future__ import annotations
 
+import json
+from unittest.mock import patch
+
 import pytest
 
 
@@ -25,3 +28,35 @@ def test_native_gemini_auxiliary_default_is_in_curated_catalog(gemini_profile):
     from hermes_cli.models import _PROVIDER_MODELS
 
     assert gemini_profile.default_aux_model in _PROVIDER_MODELS["gemini"]
+
+
+def test_native_gemini_catalog_uses_gateway_url_and_native_auth(gemini_profile):
+    payload = {
+        "models": [
+            {
+                "name": "models/ag/gemini-pro",
+                "supportedGenerationMethods": ["generateContent"],
+            },
+            {
+                "name": "models/embedding-only",
+                "supportedGenerationMethods": ["embedContent"],
+            },
+        ]
+    }
+
+    with patch("hermes_cli.urllib_security.open_credentialed_url") as opener:
+        response = opener.return_value.__enter__.return_value
+        response.read.return_value = json.dumps(payload).encode()
+
+        models = gemini_profile.fetch_models(
+            api_key="cpa-secret",
+            base_url="https://cpa.example.test/v1beta",
+        )
+
+    request = opener.call_args.args[0]
+    headers = {key.lower(): value for key, value in request.header_items()}
+    assert request.full_url == "https://cpa.example.test/v1beta/models"
+    assert headers["x-goog-api-key"] == "cpa-secret"
+    assert headers["user-agent"].startswith("hermes-cli/")
+    assert "authorization" not in headers
+    assert models == ["ag/gemini-pro"]
diff --git a/hermes_cli/auth.py b/hermes_cli/auth.py
index 502f23f..9ecbf85 100644
--- a/hermes_cli/auth.py
+++ b/hermes_cli/auth.py
@@ -7231,7 +7231,9 @@ def resolve_api_key_provider_credentials(provider_id: str) -> Dict[str, Any]:
 
     env_url = ""
     if pconfig.base_url_env_var:
-        env_url = os.getenv(pconfig.base_url_env_var, "").strip()
+        from hermes_cli.config import get_env_value_prefer_dotenv
+
+        env_url = (get_env_value_prefer_dotenv(pconfig.base_url_env_var) or "").strip()
 
     if provider_id in {"kimi-coding", "kimi-coding-cn"}:
         base_url = _resolve_kimi_base_url(api_key, pconfig.inference_base_url, env_url)
diff --git a/tests/hermes_cli/test_gemini_provider.py b/tests/hermes_cli/test_gemini_provider.py
index deb5c38..2cb091f 100644
--- a/tests/hermes_cli/test_gemini_provider.py
+++ b/tests/hermes_cli/test_gemini_provider.py
@@ -74,6 +74,28 @@ class TestGeminiAutoDetection:
 # ── Credential Resolution ──
 
 class TestGeminiCredentials:
+    def test_resolve_base_url_from_profile_secret_scope(self, monkeypatch):
+        from agent.secret_scope import reset_secret_scope, set_secret_scope
+
+        monkeypatch.setenv("GOOGLE_API_KEY", "dashboard-process-key")
+        monkeypatch.setenv(
+            "GEMINI_BASE_URL",
+            "https://dashboard-process.example/v1beta",
+        )
+        token = set_secret_scope(
+            {
+                "GOOGLE_API_KEY": "profile-key",
+                "GEMINI_BASE_URL": "https://profile.example/v1beta",
+            }
+        )
+        try:
+            creds = resolve_api_key_provider_credentials("gemini")
+        finally:
+            reset_secret_scope(token)
+
+        assert creds["api_key"] == "profile-key"
+        assert creds["base_url"] == "https://profile.example/v1beta"
+
     def test_resolve_with_google_api_key(self, monkeypatch):
         monkeypatch.setenv("GOOGLE_API_KEY", "google-secret")
         creds = resolve_api_key_provider_credentials("gemini")
